// Dashboard functionality
document.addEventListener('DOMContentLoaded', function() {
    console.log('Dashboard loaded');
    
    // Check if user is logged in (placeholder for now)
    // In the future, this will check for auth token and redirect if not logged in
    
    // Add any dashboard-specific functionality here
    // For example: loading user groups, recent expenses, etc.
});
